# Documentation build requirements for ReadTheDocs

# Sphinx and extensions
sphinx>=4.0.0
sphinx-rtd-theme>=1.0.0
myst-parser>=0.18.0

# Core dependencies (minimal versions for docs)
numpy>=1.19.0
pandas>=1.3.0
scipy>=1.7.0
matplotlib>=3.3.0
h5py>=3.1.0

# Optional dependencies that may be imported
pillow>=8.0.0
scikit-image>=0.18.0

# Mock heavy dependencies for ReadTheDocs
# These will be mocked in conf.py to avoid installation issues
# mne>=1.0.0
# pycocotools>=2.0.0
# scikit-learn>=1.0.0