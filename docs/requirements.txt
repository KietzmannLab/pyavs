# Documentation build requirements for ReadTheDocs

# Sphinx and extensions
sphinx>=7.0.0
furo>=2024.8.6
sphinx-design>=0.6.0
sphinx-copybutton>=0.5.2
myst-parser>=2.0.0

# Core dependencies (minimal versions for docs)
numpy>=1.19.0
pandas>=1.3.0
# scipy>=1.15's stats module introspects torch.Tensor at import time (via
# array-api-compat), which raises TypeError against the mocked `torch` module
# below -- pin below that until torch is either installed for real or scipy's
# mock-compatibility improves.
scipy>=1.7.0,<1.15
matplotlib>=3.3.0
seaborn>=0.11.0
h5py>=3.1.0

# Optional dependencies that may be imported
pillow>=8.0.0
scikit-image>=0.18.0
tqdm>=4.60.0
joblib>=1.0.0
requests>=2.25.0

# Mock heavy dependencies for ReadTheDocs
# These will be mocked in conf.py to avoid installation issues
# mne>=1.0.0
# pycocotools>=2.0.0
# scikit-learn>=1.0.0